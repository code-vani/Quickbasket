import { handlers } from "@/auth" // Referring to the auth.ts we just created

//handlers hamein root mein auth.ts files se mil rhe hai upr 
//handlers export kr rhe hai get aur post
//const mein horhi hai export 
export const { GET, POST } = handlers